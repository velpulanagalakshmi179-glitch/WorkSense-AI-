services:
  - type: web
    name: worksense-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn "flask_app:create_app()"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: GROQ_API_KEY
        sync: false
